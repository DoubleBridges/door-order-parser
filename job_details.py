class JobDetails:
    pass
    """
    Takes in an object from the parser where the door list has already been broken
        into sections (keys in the object) according to text height. Base text height is ~9px,
        so everything above 10px is used a section key
    """